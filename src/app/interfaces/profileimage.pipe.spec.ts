import { ProfileimagePipe } from './profileimage.pipe';

describe('ProfileimagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileimagePipe();
    expect(pipe).toBeTruthy();
  });
});
